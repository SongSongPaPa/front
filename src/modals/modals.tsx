import React from "react";

/*export const openCreateRoomModal = (setModalArgs:) => {
  setModalArgs({
    title: "Create Room",
    content: "options",
    callback: () => alert("modal closed"),
  });
};*/
